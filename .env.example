# URL del backend (FastAPI en Render)
# Copia a .env para desarrollo local
# En Vercel: Settings > Environment Variables > añade VITE_API_URL
# Si no está definida, el frontend no se conectará al backend
VITE_API_URL=https://droclven-back.onrender.com
